# OS files
.DS_Store
Thumbs.db
.env
.env.*
!.env.example

# Database backups (contain full data + password hashes — never commit)
storage/backups/*.sql
storage/backups/*.sql.gz

# Local temp/logs
*.log
/tmp/

